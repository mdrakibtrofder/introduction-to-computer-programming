/**
Write a C program that initializes variables at declaration and prints them
**/
#include <stdio.h>

int main() {
    int x = 20;
    float y = 5.5;
    printf("x=%d, y=%.2f", x, y);
    return 0;
}