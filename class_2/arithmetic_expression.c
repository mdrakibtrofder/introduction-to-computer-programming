/**
Write a C program to evaluate a simple arithmetic expression
**/
#include <stdio.h>

int main() {
    int a = 10, b = 5;
    int result = a + b * 2;
    printf("Result = %d", result);
    return 0;
}