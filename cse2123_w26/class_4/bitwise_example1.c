/**
Write a C program to demonstrate bitwise AND and OR
**/
#include <stdio.h>

int main() {
    int a = 5, b = 3;

    printf("a & b = %d\n", a & b);
    printf("a | b = %d", a | b);

    return 0;
}