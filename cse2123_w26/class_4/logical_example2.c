/**
Write a C program to demonstrate logical OR (||) and NOT (!) operator
**/
#include <stdio.h>

int main() {
    int a = 1, b = 0;

    printf("a || b = %d\n", a || b);
    printf("!a = %d", !a);

    return 0;
}