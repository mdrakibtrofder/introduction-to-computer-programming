/**
Write a C program explaining keywords (shown in comments)
**/
#include <stdio.h>

int main() {
    // int, float, return, if, else are keywords
    int a = 5;

    if(a > 0) {
        printf("a is positive");
    }

    return 0;
}