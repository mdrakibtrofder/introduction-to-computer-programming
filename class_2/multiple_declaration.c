/**
Write a C program that declares multiple variables in one line and prints them
**/
#include <stdio.h>

int main() {
    int a = 5, b = 10, c = 15;
    printf("a=%d, b=%d, c=%d", a, b, c);
    return 0;
}