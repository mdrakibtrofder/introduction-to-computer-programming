/**
Write a C program demonstrating valid variable names
**/
#include <stdio.h>

int main() {
    int age = 25;
    float total_marks = 85.5;
    int _count = 10;

    printf("age=%d, marks=%.1f, count=%d", age, total_marks, _count);
    return 0;
}