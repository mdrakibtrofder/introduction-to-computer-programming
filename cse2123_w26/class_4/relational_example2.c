/**
Write a C program to check equality and inequality
**/
#include <stdio.h>

int main() {
    int a = 15, b = 15;

    printf("a == b = %d\n", a == b);
    printf("a != b = %d", a != b);

    return 0;
}